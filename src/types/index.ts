// Export de tous les types d'authentification
export type { User, LoginResponse, LoginData, JWTPayload } from './Auth';

// Export de tous les types de quiz
export type { Quiz, DashboardMetrics, StudentInfo, QuizInfo } from './Quiz';

// Export de tous les types d'utilisateur (existants)
export type { UserCreateRequest, UserResponse, ApiErrorResponse } from './User'; 